<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SurpriSeasy" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (optional) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes styles from Shakapacker pack file %>
    <%= stylesheet_pack_tag 'application', 'data-turbo-track': 'reload' %>

    <%# Includes the main JavaScript entry point %>
    <%= javascript_pack_tag 'hello_react', 'data-turbo-track': 'reload', defer: true %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
